CREATE TABLE IF NOT EXISTS FEID_ENTITY (
    id UUID DEFAULT RANDOM_UUID() PRIMARY KEY,
    nombre_cancion VARCHAR(255) NOT NULL,
    duracion VARCHAR(10) NOT NULL,
    artista VARCHAR(255) NOT NULL
);
